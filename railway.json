{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build -w packages/web && npm run build -w packages/server"
  },
  "deploy": {
    "startCommand": "node packages/server/dist/${SERVICE_ENTRY:-index}.js"
  }
}
